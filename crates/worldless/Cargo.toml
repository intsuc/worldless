[package]
name = "worldless"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde_json = { version = "1.0", features = ["arbitrary_precision"] }
worldless-brigadier = { path = "../worldless-brigadier" }
