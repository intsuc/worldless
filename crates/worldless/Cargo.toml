[package]
name = "worldless"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
libm = "0.2"
serde = "1.0"
serde_json = { version = "1.0", features = ["arbitrary_precision", "unbounded_depth"] }
unicode-general-category = "=1.1.0"
unicode_names2 = "=1.3.0"
worldless-brigadier = { path = "../worldless-brigadier" }
